<Window x:Class="ExpenseTracker.Views.AddExpenseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Expense" Height="600" Width="480"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource AppBackgroundBrush}" 
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Add New Expense" 
                      Style="{StaticResource Heading2}"/>
            <TextBlock Text="Fill in the details below" 
                      Style="{StaticResource BodyText}"
                      Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Form -->
        <Border Grid.Row="1" Style="{StaticResource DashboardCard}" Padding="24">
            <StackPanel>
                <!-- Amount -->
                <TextBlock Text="Amount *" Style="{StaticResource ModernLabel}"/>
                <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" 
                        Style="{StaticResource ModernTextBox}"
                        Margin="0,8,0,16"/>

                <!-- Category -->
                <TextBlock Text="Category *" Style="{StaticResource ModernLabel}"/>
                <ComboBox ItemsSource="{Binding Categories}" 
                         SelectedValue="{Binding SelectedCategoryId}"
                         SelectedValuePath="Id"
                         DisplayMemberPath="Name"
                         Style="{StaticResource ModernComboBox}"
                         Margin="0,8,0,16"/>

                <!-- Description -->
                <TextBlock Text="Description" Style="{StaticResource ModernLabel}"/>
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                        Style="{StaticResource ModernTextBox}"
                        Height="80" TextWrapping="Wrap" AcceptsReturn="True"
                        VerticalScrollBarVisibility="Auto"
                        Margin="0,8,0,16"/>

                <!-- Date -->
                <TextBlock Text="Date *" Style="{StaticResource ModernLabel}"/>
                <DatePicker SelectedDate="{Binding Date}" 
                           Style="{StaticResource ModernDatePicker}"
                           Margin="0,8,0,16"/>

                <!-- Payment Method -->
                <TextBlock Text="Payment Method" Style="{StaticResource ModernLabel}"/>
                <ComboBox Text="{Binding PaymentMethod}" IsEditable="True"
                         Style="{StaticResource ModernComboBox}"
                         Margin="0,8,0,0">
                    <ComboBoxItem Content="Cash"/>
                    <ComboBoxItem Content="Credit Card"/>
                    <ComboBoxItem Content="Debit Card"/>
                    <ComboBoxItem Content="UPI"/>
                    <ComboBoxItem Content="Net Banking"/>
                </ComboBox>
            </StackPanel>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" 
                   Orientation="Horizontal" 
                   HorizontalAlignment="Right" 
                   Margin="0,24,0,0">
            <Button Content="Cancel" 
                   Command="{Binding CancelCommand}"
                   Style="{StaticResource SecondaryButton}"
                   Margin="0,0,8,0"/>
            <Button Content="Save Expense" 
                   Command="{Binding SaveCommand}"
                   Style="{StaticResource PrimaryButton}"/>
        </StackPanel>
    </Grid>
</Window>
